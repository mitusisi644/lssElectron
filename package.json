{
	"scripts": {
	    "packageWin": "electron-packager ./dist/ --platform=win32 --arch=x64 --overwrite",
	    "packageDarwin": "electron-packager ./dist/ --platform=darwin --arch=x64 --app-version=1.0.1 --overwrite"
	},
	"devDependencies": {
		"electron": "^2.0.2",
		"electron-packager": "^12.1.0"
	}
}
